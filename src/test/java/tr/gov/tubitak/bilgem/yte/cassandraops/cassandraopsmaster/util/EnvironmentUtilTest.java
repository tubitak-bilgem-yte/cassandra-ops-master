package tr.gov.tubitak.bilgem.yte.cassandraops.cassandraopsmaster.util;

import org.junit.Test;

public class EnvironmentUtilTest {
    @Test
    public void getNodeEntityIpAddresses_untrimmedIpAddresses_returnsTrimmedIpAddresses() {
        /*
        Environment environmentStub = mock(Environment.class);
        when(environmentStub.getProperty("nodeagent.ipaddresses")).thenReturn("ip1, ip2");
        EnvironmentUtil environmentUtil = new EnvironmentUtil();
        environmentUtil.setEnvironment(environmentStub);
        assertArrayEquals(new String[] {"ip1", "ip2"}, environmentUtil.getNodeAgentIpAddresses());*/
    }
}
